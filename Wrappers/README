
Go into the directory you are interested in - 

 base - GNUstep base library java wrappers;
 libFoundation - libFoundation java wrappers;
 gui - GNUstep gui library java wrappers <not working at the moment>

Type `make' to create and compile them, and `make install' (as root)
to install them.
